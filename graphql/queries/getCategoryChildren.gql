query getCategoryChildren($slug: ID!) {
  category(id: $slug, idType: SLUG) {
    id
    name
    slug
    children {
      nodes {
        id
        name
        slug
        children {
          nodes {
            id
            name
            slug
          }
        }
      }
    }
  }
}
