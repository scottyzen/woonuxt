query getCategoryChildren($slug: ID!) {
  productCategory(id: $slug, idType: SLUG) {
    id
    name
    slug
    children(first: 50) {
      nodes {
        id
        name
        slug
        children(first: 50) {
          nodes {
            id
            name
            slug
          }
        }
      }
    }
  }
}
