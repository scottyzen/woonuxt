query GetFooterMenus {
  footer1: menuItems(where: { slug: "footer-1" }) {
    nodes {
      id
      label
      url
    }
  }
  footer2: menuItems(where: { slug: "footer-2" }) {
    nodes {
      id
      label
      url
    }
  }
  footer3: menuItems(where: { slug: "footer-3" }) {
    nodes {
      id
      label
      url
    }
  }
}
