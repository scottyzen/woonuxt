query getProductCategories($first: Int = 20, $include: [ID]) {
  productCategories(
    first: $first
    where: { 
      orderby: COUNT
      order: DESC
      hideEmpty: true
      include: $include
    }
  ) {
    nodes {
      ...ProductCategory
    }
  }
}
