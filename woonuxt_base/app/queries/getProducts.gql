query getProducts(
  $first: Int = 12, 
  $after: String = "",
  $orderby: ProductsOrderByEnum = DATE
) {
  products(first: $first, after: $after, where: { orderby: $orderby }) {
    pageInfo {
      hasNextPage
      endCursor
    }
    edges {
      node {
        id
        databaseId
        name
        slug
        type
        onSale
        shortDescription
        image {
          id
          sourceUrl
          altText
        }
        ... on SimpleProduct {
          price
          regularPrice
          stockStatus
        }
        ... on VariableProduct {
          price
          regularPrice
          stockStatus
        }
      }
    }
  }
}
